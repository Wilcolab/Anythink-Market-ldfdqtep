/**
 * Write a function called toCamelCase that takes a string parameter and returns 
 * the string converted to camelCase. The function should handle strings with spaces,
 * hyphens, and underscores as word separators. For example:
 * 
 * toCamelCase("hello world") should return "helloWorld"
 * toCamelCase("hello-world") should return "helloWorld"
 * toCamelCase("hello_world") should return "helloWorld"
 * 
 * The first word should be lowercase, and each subsequent word should have its
 * first letter capitalized with no separators between words.
 */
